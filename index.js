// add solution here
function theBeatlesPlay(musicians, instruments) {
  var beatlesInstruments = [];
  for (let i = 0; i < musicians.length; num++) {
   beatlesInstruments.push(musicians[num] + " plays " + instruments[num]); 
  }
  return beatlesInstruments;
}